### Browserify + Express + JQuery + Backbone

Take a look at the package.json. The script section has a bunch of handle tools
to use while in development.

1. npm run build
2. npm run watch
3. npm start

